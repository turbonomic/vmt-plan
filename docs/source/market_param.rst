Market Parameters
-----------------

The following market parameters are supported as keyword arguments. Unless
otherwise stated, all are independent and optional.

* ignore_constraints (`bool`): Ignores cluster boundaries and utilizes the plan market to decide VM placements.
* plan_market_name (`str`): Desired market name for the plan. One is generated if left blank.